// Created by Mike Schmoyer
// Jul 31, 2018

// This is the necessary core for a rule using the "promise" type. 

var determineRule = (dataContext, customerData) => {
  return new Promise((resolve, reject) => {
    // resolve(responseObject);
    // reject(responseObject);
  });
}

module.exports = {
  determineRule
};
